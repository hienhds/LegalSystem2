spring.application.name=case-service
server.port=8085

# 1. Database Configuration (Chu?n 3336 và 231123 theo Docker c?a b?n)
spring.datasource.url=jdbc:mysql://127.0.0.1:3336/case_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=231123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 3. Eureka Discovery
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# 4. gRPC Client Configuration (S?A L?I FAILED TO CREATE CHANNEL)
# Ép gRPC s? d?ng c?u hình static, không dùng Discovery ?? tránh l?i hostname trong Docker
grpc.client.file-service.address=static://127.0.0.1:9090
grpc.client.file-service.negotiation-type=plaintext
# Thêm dòng này ?? ch?c ch?n không b? timeout lúc kh?i t?o
grpc.client.file-service.enable-keep-alive=true

# 5. Jackson (Timezone)
spring.jackson.time-zone=Asia/Ho_Chi_Minh